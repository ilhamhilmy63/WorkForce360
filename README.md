# WorkForce360 Frontend

Responsive React prototype for the WorkForce360 workforce-management platform.

```bash
npm install
npm run dev
```

This frontend-only build uses local mock data. It includes a dashboard, employee directory, interactive leave review queue, module placeholders, and AI workflow review.
