import React,{useState}from'react';import{createRoot}from'react-dom/client';import TripCraftApp from'./TripCraftApp';import TripCraftAuth from'./TripCraftAuth';import'./styles.css';import'./app.css';import'./tripcraft.css';
function Root(){const key='tripcraft_session',[session,setSession]=useState(()=>localStorage.getItem(key)||sessionStorage.getItem(key));const authenticated=(result,remember)=>{const value=JSON.stringify(result);(remember?localStorage:sessionStorage).setItem(key,value);setSession(value)};const logout=()=>{localStorage.removeItem(key);sessionStorage.removeItem(key);setSession(null)};return session?<TripCraftApp session={JSON.parse(session)} onLogout={logout}/>:<TripCraftAuth onAuthenticated={authenticated}/>}
createRoot(document.getElementById('root')).render(<React.StrictMode><Root/></React.StrictMode>);
